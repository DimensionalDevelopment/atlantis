modLoader = "javafml"
loaderVersion = "${loader_version_range}"
license = "${mod_license}"
issueTrackerURL="https://github.com/Exploding-Creeper/atlantis/issues"

[[mods]]
modId = "${mod_id}"
version = "${mod_version}"
displayName = "${mod_name}"
displayURL="https://github.com/Exploding-Creeper/atlantis"
logoFile= "logo.png"
credits="TelepathicGrunt for his help with the JSON files, WaterPicker for his work on the fabric branch, LudoCrypt and Firel for the music on the music discs for atlantis, She's Long for multiple textures, models, and jsons for assets, and LimeShulkerBox for the Atlantean Portal and for removing the need for effects in the dimension."
authors = "${mod_authors}"
description = '''An Underwater Dimension Mod.'''
[[mixins]]
config = "${mod_id}.mixins.json"


[[accessTransformers]]
file="META-INF/accesstransformer.cfg"

[[dependencies."${mod_id}"]]
modId = "neoforge"
type = "required"
versionRange = "${neo_version_range}"
ordering = "NONE"
side = "BOTH"

[[dependencies."${mod_id}"]]
modId = "minecraft"
type = "required"
versionRange = "${minecraft_version_range}"
ordering = "NONE"
side = "BOTH"

[[dependencies."${mod_id}"]]
modId="justhammers"
type = "required"
versionRange="[1.+,)"
ordering="NONE"
side="BOTH"

[[dependencies."${mod_id}"]]
modId="geckolib3"
type = "required"
versionRange="[4.2.2,)"
ordering="NONE"
side="BOTH"